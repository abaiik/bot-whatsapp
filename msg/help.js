exports.Help = (prefix) => {
    return `
*MENU OPTIONAL*
⤷ ${prefix}ping = speed test bot

*BUG MENU*
⤷ ${prefix}. = group bug
⤷ ${prefix}.. = send troli
⤷ ${prefix}... = send troli nomor
    `
}

exports.afkMentioned = (pushname, getReason) => {
    return `Hai ${pushname} 👋\n\nMohon tinggalkan pesan disini dan saya akan segera kembali online!.\n\nI'm busy right now\nplease leave a message here and i will reply right away!\n\n*Alasanya :* ${getReason}`
}

exports.photoOxy = (prefix) => {
    return `
*MENU PHOTO OXY*

⤷ ${prefix}retrolol ? text
⤷ ${prefix}harrypotter ? text
⤷ ${prefix}romantic ? text
⤷ ${prefix}smoke ? text
⤷ ${prefix}shadow ? text
⤷ ${prefix}burnpapper ? text
⤷ ${prefix}lovemsg ? text
⤷ ${prefix}naruto ? text
⤷ ${prefix}doubleheart ? text
⤷ ${prefix}coffecup ? text
⤷ ${prefix}lovetext ? text
⤷ ${prefix}butterfly ? text

⤷ ${prefix}pubg ? text, text2
⤷ ${prefix}battlefield ? text, text2
⤷ ${prefix}googlesuges ? text, text2
⤷ ${prefix}glitch ? text, text2
    `
}

exports.textPro = (prefix) => {
    return `
*MENU TEXT PRO*

⤷ ${prefix}neondevil ? text
⤷ ${prefix}matrix ? text
⤷ ${prefix}firework ? text
⤷ ${prefix}blackpink ? text
⤷ ${prefix}dropwater ? text
⤷ ${prefix}christmas ? text
⤷ ${prefix}3dgradient ? text

⤷ ${prefix}marvel ? text, text2
⤷ ${prefix}logowolf ? text, text2
⤷ ${prefix}logowolf2 ? text, text2
⤷ ${prefix}pornhub ? text, text2
    `
}
